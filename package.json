{
  "name": "gex-proxy",
  "version": "1.0.0",
  "description": "Simple proxy server that keeps the Polygon.io API key on the server side.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
